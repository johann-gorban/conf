#include "parser.hpp"

std::vector<config::token_ptr> config::processing::parsing::Parser::parse(const std::vector<token_ptr> &tokens) const {
    std::vector<token_ptr> parsed_tokens;

    return parsed_tokens;
}